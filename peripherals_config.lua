-- Name of reactor stabilizer
local reactorName = "top"
-- Name of flux gate to control output energy
local outputGateName = "right"
-- Name of flux gate to control input energy
local inputGateName = "back"
-- Name of monitor to display information
local monitorName = "monitor_0"

return {
    reactorName = reactorName,
    outputGateName = outputGateName,
    inputGateName = inputGateName,
    monitorName = monitorName
}